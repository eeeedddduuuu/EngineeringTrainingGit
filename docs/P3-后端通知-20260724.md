# P3 状态同步 — 组员待办清单

**发自：P3（后端开发）**  
**日期：2026年7月24日（Day 1）**  
**分支：`feature/backend`**

---

## ✅ P3 已完成事项（供全组知悉）

| 序号 | 事项 | 状态 | 说明 |
|---|---|---|---|
| 1 | 数据库全部6张表 | ✅ 完成 | users, sessions, schemes, agent_logs, knowledge_items, reviews |
| 2 | 15个API端点 | ✅ 完成 | 认证/创作/方案/历史/导出/统计/知识库/审核 |
| 3 | Mock Agent工作流 | ✅ 完成 | 4个Agent串联模拟（trend→script→review→strategy） |
| 4 | 认证中间件 | ✅ 完成 | JWT + OAuth2PasswordBearer，全业务接口强制认证 |
| 5 | 数据库初始化脚本 | ✅ 完成 | `python backend/init_db.py` 一键建表+种子数据 |
| 6 | 测试账号 | ✅ 就绪 | test / 123456 |
| 7 | 知识库种子数据 | ✅ 就绪 | 10条样例（护肤/美食/数码/旅行/穿搭） |

---

## 📋 各组员待办事项

### 🔴 P1（PM）— 需确认

- [ ] 审核 P3 的 API 接口实现是否偏离需求
- [ ] 更新 `docs/progress_log.md` 将 P3 状态改为已完成
- [ ] 确认 Day 1 中午的接口 JSON Schema 已对齐（`docs/api_schema.md` 已有一致实现）
- [ ] 准备明天（Day 2）首次联调的组织：P2↔P3 注册登录流程打通

### 🔴 P2（前端）— 需对接

- [ ] **拉取最新代码**：`git pull origin feature/backend`（待P3推送后）
- [ ] **注册登录联调**：
  - `POST /api/auth/register` → 返回 `{id, username, message}`
  - `POST /api/auth/login` → 返回 `{access_token, token_type, username}`
  - `GET /api/auth/me` → Header 带 `Authorization: Bearer <token>`
- [ ] **创作工作台对接**：
  - `POST /api/creation/start` → 返回 `task_id`，前端轮询 `GET /api/task/{id}/status`
  - 轮询间隔建议：1秒
- [ ] **方案浏览页对接**：
  - `GET /api/schemes?session_id=` → 获取会话下所有方案
  - `GET /api/scheme/{id}` → 获取单个方案详情
  - `POST /api/schemes/compare` → A/B对比

### 🔴 P4（Agent）— 需对接

- [ ] **接口对齐确认**：Agent 输入输出 JSON Schema 与 `docs/api_schema.md` 一致
- [ ] **Agent 调用日志对齐**：`agent_logs` 表字段确认（已在 models/business.py 定义）
  - agent_name / input_json / output_json / tools_called / latency_ms / tokens_used / status
- [ ] **异步任务对接**：当前P3使用 Mock Agent 工作流（模拟4个Agent），P4完成后替换为真实调用
- [ ] **P4 回复**：确认 `p4-agent/README.md` 中的接口格式无冲突

### 🔴 P5（数据/知识库）— 需对接

- [ ] **知识库检索接口对齐**：
  - P3 已实现 `GET /api/knowledge/search?q=&top_k=5`
  - 响应格式：`{query, results: [{id, title, content_snippet, platform, tags, source, similarity}]}`
  - 当前为简易关键词搜索，P5 完成后可替换为 Chroma 向量检索
- [ ] **统计数据接口对齐**：
  - `GET /api/stats/samples` → 返回主题分布、平台分布、月度趋势
  - 当前返回 Mock 数据，P5 完善知识库后更新

---

## 🔗 关键信息速查

| 项目 | 信息 |
|---|---|
| API Base URL | `http://localhost:8000` |
| Swagger 文档 | `http://localhost:8000/docs` |
| 测试账号 | `test` / `123456` |
| 认证方式 | Header `Authorization: Bearer <JWT_TOKEN>` |
| 错误格式 | `{"error": "...", "detail": "..."}` |
| 后端启动 | `cd backend && uvicorn app.main:app --reload` |
| 数据库初始化 | `cd backend && python init_db.py` |

---

## ⚠️ 注意事项

1. **所有业务接口**（除 register/login）都强制 JWT 认证，前端务必在请求头中带 token
2. **创作任务是异步的**：`/api/creation/start` 立即返回 task_id，前端需轮询 `/api/task/{id}/status`
3. **CORS 已全部放通**（`allow_origins=["*"]`），本地开发无跨域问题
4. **当前为 Mock 模式**：Agent 工作流返回模拟数据，P4 真实 Agent 就绪后再对接
5. **Git 提交规范**：`feat(module): description`，每人≥5次提交

---

*清单由 P3 于 2026-07-24 生成，各组员收到后请回复确认*
